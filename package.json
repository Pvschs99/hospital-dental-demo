{
  "name": "incisors-dental-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:dev": "echo 'Static site — no build needed'",
    "build": "echo 'Static site — no build needed'",
    "dev": "node serve.cjs"
  }
}
